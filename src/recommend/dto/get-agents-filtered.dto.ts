// export class GetAgentsFilteredDto {
//   state: string;
//   clientType: string;

//   // Get the minimum number of referrals sent from req.query
//   minReferralsSent = parseInt(req.query.minReferralsSent as string);

//   // Get the minimum number of houses sold from req.query
//   minHousesSold = parseInt(req.query.minHousesSold as string);

//   minYearsOfExperience = parseInt(req.query.minYearsOfExperience as string);
// }
